<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:SocialMediaApp.Views"
       x:Class="SocialMediaApp.AppShell"
       Shell.NavBarHasShadow="False"
       Shell.BackgroundColor="White"
       Shell.ForegroundColor="#262626"
       Shell.TitleColor="#262626"
       Shell.TabBarBackgroundColor="White"
       Shell.TabBarForegroundColor="#262626"
       Shell.TabBarUnselectedColor="#8E8E8E"
       Shell.TabBarTitleColor="#262626">

    <ShellContent Title="Login"
                  ContentTemplate="{DataTemplate views:LoginPage}"
                  Route="LoginPage"
                  Shell.TabBarIsVisible="False"/>

    <ShellContent Title="Register"
                  ContentTemplate="{DataTemplate views:RegisterPage}"
                  Route="RegisterPage"
                  Shell.TabBarIsVisible="False"/>

    <TabBar>
        <ShellContent Title="Home"
                      Icon="home.png"
                      ContentTemplate="{DataTemplate views:HomePage}"
                      Route="HomePage"/>
        <ShellContent Title="Profile"
                      Icon="profile.png"
                      ContentTemplate="{DataTemplate views:ProfilePage}"
                      Route="ProfilePage"/>
    </TabBar>

</Shell>
